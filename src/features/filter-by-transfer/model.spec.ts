describe('filters', () => {
  describe('activation', () => {
    it.todo('check active all filters');
    it.todo('when clicking on the active filter, except "all" to disable it');
    it.todo('when clicked on the active filter "all", disable others filters');
    it.todo('if any filter is inactive, disabled filter "all"');
    it.todo('if all filters are active except "all", then turn it on');
    it.todo('if disable the filter "all", then everyone will be turned off');
  });

  describe('events filters', () => {
    it.todo('filtering tickets by 1,2 transfers');
    it.todo('filtering tickets by all');
    it.todo('filtering without transplants');
  });
});
